# transcribe.py
"""
Transcriber component for converting audio to text using whisper.cpp.

Responsibilities:
- Invoke whisper-cpp CLI with audio file
- Parse transcript output
- Return raw transcript text

Requirements:
- whisper.cpp installed (brew install whisper-cpp)
- Whisper model downloaded (e.g., ggml-base.en.bin)
- Audio must be 16kHz mono WAV
"""

# TODO: Implement Transcriber class
# TODO: Build whisper-cpp CLI command with appropriate args
# TODO: Use subprocess.run() with check=True
# TODO: Read and parse the output .txt file
# TODO: Handle errors gracefully (log and return empty string)
